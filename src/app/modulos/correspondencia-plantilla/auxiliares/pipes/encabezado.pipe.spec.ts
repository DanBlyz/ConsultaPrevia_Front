import { EncabezadoPipe } from './encabezado.pipe';

describe('EncabezadoPipe', () => {
  it('create an instance', () => {
    const pipe = new EncabezadoPipe();
    expect(pipe).toBeTruthy();
  });
});
