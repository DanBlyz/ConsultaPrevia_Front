import { ParticipantesPipe } from './participantes.pipe';

describe('ParticipantesPipe', () => {
  it('create an instance', () => {
    const pipe = new ParticipantesPipe();
    expect(pipe).toBeTruthy();
  });
});
